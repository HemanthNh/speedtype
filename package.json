{
  "name": "aswin-typing-monitor",
  "version": "5.8.0",
  "private": true,
  "description": "Testing-focused typing monitor with viewport-fit side-by-side code editors, stable synchronized scrolling, accessible mistake heatmap, progression, Telegram coaching reports and resilient local persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/exercise-bank.js && node --check public/core.js && node --check public/app.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18 <25"
  }
}
