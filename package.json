{
  "name": "aswin-typing-monitor",
  "version": "5.6.0",
  "private": true,
  "description": "Testing-focused typing monitor with accessible mistake heatmap, responsive IDE-style workspace, readable code bank, progression, diagnostics, Telegram coaching reports and resilient local persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/exercise-bank.js && node --check public/core.js && node --check public/app.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18 <25"
  }
}
