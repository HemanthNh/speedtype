{
  "name": "aswin-typing-monitor",
  "version": "6.5.0",
  "private": true,
  "description": "Testing-focused typing coach with PostgreSQL persistence, Monkeytype-style ghost editor, teaching-first exercise bank, visual analytics, speed quest gamification, progression and Telegram coaching reports",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/exercise-bank.js && node --check public/core.js && node --check public/app.js",
    "test": "node --test tests/*.test.js",
    "migrate:json": "node scripts/migrate-json-to-postgres.js",
    "db:check": "node scripts/check-database.js"
  },
  "engines": {
    "node": ">=18 <25"
  },
  "dependencies": {
    "pg": "^8.0.0"
  }
}
