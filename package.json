{
  "name": "aswin-typing-monitor",
  "version": "6.1.0",
  "private": true,
  "description": "Testing-focused typing monitor with minimal visual analytics, ranked color-coded mistake analysis, viewport-fit code editors, progression, Telegram coaching reports and resilient local persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/exercise-bank.js && node --check public/core.js && node --check public/app.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18 <25"
  }
}
